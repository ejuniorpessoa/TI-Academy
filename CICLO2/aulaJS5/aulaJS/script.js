//new é um obejto que possui suas propriedas e conseguimos manipular datas exe: var data = new Date( );
//              Formatação de Data    

import {msgLog} from "./modulos/modulos.js"// "./" referencia raiz


window.onload = function(){

    msgLog("Olá");
    /* var data = new Date();
    semana = ["domingo", "segunda", "terça", "quarta", "quinta", "sexta"]//array para dias da semana!

    console.log(semana[data.getDay()]); // tras o dia da semana de acordo com o array
    //toLocaleString recebe alguns parâmetros
    var dta = data.toLocaleString("pt-BR", {year:"numeric", month:"2-digit", day:"2-digit"})//<---{ } recebe informações como objeto!
    console.log(dta);
    //acrescentar data ou dias\/
    //var dta = data.setDate(data.getDate()+3);    
    //console.log(dta);// <--- usa milésimo de segundos

 */
/* 
    var n = 0;
    // saber se o valor que vai chegar até o processo se é realmente um número
    try{// aqui é o que eu quero que execute
        n = Number(n);
        if(isNaN(n)) throw " Este valor não é um número";// <--- excessão

    } catch(erro){ // erro passa pelo "catch"
    console.log(`Erro: ${erro}`) // personaliza o erro para enviar ao usuário
    } finally{
        console.log("Terminando");
    }
     */
    
}